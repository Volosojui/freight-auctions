## 1. Инициализация проекта и тулинг

- [x] 1.1 Создать Vite-проект React + TypeScript (strict) в корне репозитория
- [x] 1.2 Настроить `tsconfig` со строгим режимом и path-алиасами под FSD-слои (`@app`, `@pages`, `@widgets`, `@features`, `@entities`, `@shared`) и продублировать алиасы в конфиге Vite
- [x] 1.3 Добавить ESLint + Prettier; включить правило границ импортов FSD (импорт только «вниз» по слоям, между слайсами — через публичный `index.ts`)
- [x] 1.4 Добавить проверку именования: файлы React-компонентов обязаны иметь суффикс `*.component.tsx`
- [x] 1.5 Настроить Vitest + jsdom и npm-скрипты (`dev`, `build`, `preview`, `lint`, `test`)

## 2. Каркас FSD и app-слой

- [x] 2.1 Создать каталоги слоёв `src/{app,pages,widgets,features,entities,shared}` с `index.ts`-барьерами
- [x] 2.2 Реализовать точку входа `src/app` с монтированием в единый root
- [x] 2.3 Подключить провайдер TanStack Query с единым `QueryClient` (+ devtools в dev)
- [x] 2.4 Инициализировать TanStack Router (+ devtools в dev) и обернуть приложение роутером
- [x] 2.5 Добавить MobX (`mobx` + `mobx-react-lite`); задокументировать в `shared` паттерн стор-фабрики и `observer`

## 3. App shell и базовый UI

- [x] 3.1 Реализовать корневой адаптивный layout (шапка + контентная область) без горизонтального скролла на mobile
- [x] 3.2 Добавить минимальный `shared/ui`: кнопка, карточка, skeleton-примитив, спиннер, toast-хост
- [x] 3.3 Настроить корневой error boundary с fallback-состоянием
- [x] 3.4 Реализовать состояние not-found для неизвестного маршрута с возвратом к списку

## 4. Роут-скелет

- [x] 4.1 Зарегистрировать маршрут списка `/` (заглушка контента)
- [x] 4.2 Зарегистрировать маршрут детальной `/auctions/$auctionUuid` (заглушка), прокинуть `auctionUuid` в компонент маршрута
- [x] 4.3 Проверить навигацию между маршрутами внутренними ссылками без полной перезагрузки

## 5. Интеграция MSW (bootstrap)

- [x] 5.1 Установить MSW и сгенерировать service worker (`public/`)
- [x] 5.2 Создать `shared/api/mock` c пустым набором хендлеров и фабрикой воркера (наполнение — в change `api-and-mocks`)
- [x] 5.3 Стартовать воркер только при `import.meta.env.DEV` (и в тест-сетапе) до рендера; убедиться, что ветка отсутствует в production
- [x] 5.4 Настроить тест-сетап Vitest на запуск/остановку MSW-сервера

## 6. Проверка и валидация

- [x] 6.1 Smoke-тест: app shell монтируется, рендерится layout, доступен `QueryClient` в дереве
- [x] 6.2 Проверить сборку production и отсутствие мок-кода/данных в бандле
- [x] 6.3 Прогнать `lint` и `test`; убедиться, что `npm run dev` поднимает рабочий скелет со всеми провайдерами
- [x] 6.4 `openspec validate --change bootstrap-app --strict` — без ошибок
