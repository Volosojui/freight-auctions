## 1. Схема и валидация ставки

- [ ] 1.1 Фабрика Zod-схемы `makeBetSchema({min,max,step})`: `price>0`, диапазон и кратность шагу (аккуратно с плавающей точкой)
- [ ] 1.2 Unit-тесты схемы: пустая/`<=0`, вне диапазона, не кратна шагу, валидная граница

## 2. Форма и маршрут

- [ ] 2.1 Адресуемый маршрут/модалка режима ставки, открывается ссылкой из карточки списка и с детальной
- [ ] 2.2 Форма RHF + `zodResolver`; поле цены; подсказка по доступной цене и шагу из detail DTO
- [ ] 2.3 Доступность формы по `trading.can_set_bet` (disabled/скрыто + состояние недоступности при прямом переходе)
- [ ] 2.4 MobX-стор состояния открытия/сабмита фичи

## 3. Мутация и обработка ошибок

- [ ] 3.1 Mutation-хук `POST /auctions/{auctionUuid}/bets` c телом `SetBetRequest`
- [ ] 3.2 Обработка 422 `ValidationProblem`: `errors[].field` → `setError` в RHF; общая ошибка → error-toast
- [ ] 3.3 success-toast при успехе; error-toast для 404/503/бизнес-`ProblemDetail`
- [ ] 3.4 Инвалидация ключей: список, `['auction', uuid]`, `['auction', uuid, 'bets']`

## 4. Мутация MSW-store (расширение mock-backend)

- [ ] 4.1 Чистые функции обновления store при успешной ставке: добавить `BetItem`, пересчитать `place`/лидерство
- [ ] 4.2 Обновить `trading.price.current`, `trading.your` (`bet`, `last_bet`) и торговый статус пользователя (`status_mobile`)
- [ ] 4.3 Подключить мутацию в `setBet`-хендлер; 422 для `price<=0`/нарушений
- [ ] 4.4 Unit-тесты функций мутации store (детерминизм через `reset()`)

## 5. Приёмочные тесты (сценарии спеки)

- [ ] 5.1 Integration (jsdom+MSW): доступность формы по `can_set_bet`; валидация цены (пустая/`<=0`/вне диапазона/не кратна шагу); подсказка по доступной цене и шагу
- [ ] 5.2 Integration: 422 `ValidationProblem` → полевые ошибки в форме + error-toast; иная ошибка → error-toast
- [ ] 5.3 Integration: успех → success-toast + инвалидация; после ставки обновлены текущая цена, «моя ставка», список ставок (мутация MSW-store)
- [ ] 5.4 E2E (Playwright): ключевой сквозной путь — открыть форму по ссылке, поставить ставку, увидеть обновление цены/статуса/ставок и success-toast

## 6. Валидация

- [ ] 6.1 Прогнать `lint`, `test` (unit+integration) и `test:e2e` — всё зелёное
- [ ] 6.2 `openspec validate place-bet --strict` — без ошибок
