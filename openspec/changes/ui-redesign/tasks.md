## 1. Токен-система и темы

- [x] 1.1 Обновить `src/app/styles/global.css`: семантические токены (палитра/типографика/spacing/radius/elevation) по `design.md`; светлая тема на `:root`
- [x] 1.2 Тёмная тема: переопределение токенов в `@media (prefers-color-scheme: dark)` (guarded `:root:not([data-theme="light"])`) и `:root[data-theme="dark"]`; ни один цвет не определён только в media-блоке
- [x] 1.3 Табличные цифры для данных/цен (`font-variant-numeric: tabular-nums`); базовые типографические классы
- [x] 1.4 `@media (prefers-reduced-motion: reduce)` — отключение переходов/анимаций

## 2. Переключатель темы

- [x] 2.1 Модуль `shared` theme-controller: чтение/запись `localStorage['theme']`, дефолт по системе, установка `data-theme` на `<html>`; React-хук
- [x] 2.2 Инлайн-скрипт в `index.html` для ранней установки темы (анти-FOUC)
- [x] 2.3 Кнопка-переключатель в шапке app shell (иконка, `aria-label`, `aria-pressed`/состояние)

## 3. Переоформление экранов (сохранять data-testid/роли/текст)

- [x] 3.1 App shell: шапка/навигация/контейнер, слот переключателя темы, ToastHost-стили
- [x] 3.2 `shared/ui`: Button (варианты/состояния/focus-visible), Card, Skeleton, Spinner, Toast — на новых токенах
- [x] 3.3 Список: плотные карточки аукциона, иерархия, tabular-цены, badges статусов; skeleton/empty/error
- [x] 3.4 Фильтры: поля/чекбокс-группы как chips, метки/helper/focus/ошибки
- [x] 3.5 Детальная: секции, цены (tabular), tabs (индикатор + `aria-selected`), состояния скрытия DTO
- [x] 3.6 Вкладка ставок: строки ставок, badges (победитель/отменён), число участников, empty/hidden
- [x] 3.7 Модалка ставки: layout, focus-trap, автофокус, Esc-close, возврат фокуса, анимация из триггера (reduced-motion)

## 4. Доступность

- [x] 4.1 `:focus-visible` кольца на всех интерактивных; проверка клавиатурной навигации сквозь ключевые потоки
- [x] 4.2 Контраст пар ≥ AA в светлой и тёмной темах (проверить статусы/muted/ссылки)
- [x] 4.3 ARIA-имена у иконочных кнопок; статусы не только цветом (иконка/текст)

## 5. Приёмочные тесты (сценарии спеки)

- [x] 5.1 Integration: переключение темы меняет `data-theme` и сохраняется в `localStorage`; дефолт следует системе
- [x] 5.2 Integration: модалка ставки закрывается по Esc и возвращает фокус; фокус остаётся внутри при открытии
- [x] 5.3 Регрессия: вся существующая свита (93 unit/integration + 17 e2e) остаётся зелёной без изменения проверок по существу
- [x] 5.4 E2E: переключатель темы виден и работает; ключевые экраны отображаются в обеих темах

## 6. Валидация

- [x] 6.1 Прогнать `lint`, `test` (unit+integration) и `test:e2e` — всё зелёное; проверить 375px и обе темы
- [x] 6.2 `openspec validate ui-redesign --strict` — без ошибок

## 7. Ревизия v2 (иконки, drawer, ритм)

- [x] 7.1 Добавить `lucide-react`; иконки в шапку/бренд, primary-действия, мета-строки карточки, статусы ставок, тосты, переключатель темы (декоративные — `aria-hidden`)
- [x] 7.2 Компонент Drawer в `shared/ui` (slide-over справа, scrim, focus-trap, Esc-close, возврат фокуса, reduced-motion)
- [x] 7.3 Фильтры: убрать колонку, список на всю ширину; панель над списком — кнопка «Фильтры» (иконка + счётчик активных) + активные фильтры чипсами с удалением; форма фильтров внутри Drawer
- [x] 7.4 Увеличить вертикальный ритм: токены секций (16/24/32), крупнее `gap` списка, просторнее мета карточки, строки ставок и секции детальной
- [x] 7.5 Обновить тесты: integration list открывает drawer перед взаимодействием с фильтрами; тест кнопки/счётчика фильтров и открытия drawer; вся свита зелёная
- [x] 7.6 Прогнать `lint`/`test`/`test:e2e`; проверить 375px и обе темы; `openspec validate ui-redesign --strict`
