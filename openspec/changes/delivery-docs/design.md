## Context

Финальный change поставки: только документация и сверка. Архитектурных решений нет — приложение уже построено предыдущими change'ами. См. `proposal.md` — Why. Раздел существует, чтобы разблокировать `tasks`; содержательных технических развилок здесь нет.

## Goals / Non-Goals

**Goals:**
- Зафиксировать способ запуска и проверки (README) и рефлексию по AI (AI_USAGE.md) по пунктам ТЗ.
- Финальная сверка конвенций и сборки.

**Non-Goals:**
- Любые изменения поведения приложения, новые зависимости или рефакторинг.

## Decisions

- Документацию держим в корне репозитория (`README.md`, `AI_USAGE.md`) — ожидаемое место для проверяющего.
- README описывает реальный, проверенный способ запуска и перечисляет пройденные сценарии и ограничения честно (в т.ч. что НЕ покрыто).

## Risks / Trade-offs

- [README расходится с фактическим состоянием] → Писать после финального прогона `lint`/`test`/`dev`/`build`, сверяясь с результатом.
